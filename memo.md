メモ
--- 

'Canvas' is declared but its value is never read.ts(6133)
Cannot find module '@react-three/fiber' or its corresponding type declarations.

install

```
npm install three @types/three @react-three/fiber @react-three/drei
```

mesh.rotation.y += 0.1 で回せるが、ルービックキューブには厄介な特徴がある
ルービックキューブを回転させるのは難しい
→ ある面を回すときに、側面も正しく回転させなければならない

Pivot(回転軸グループ)の一時アタッチを使う

・通常状態
シーン→ 27個のキューブレット

・回転中（上面 Y=1 を回す）
シーン→動かさない18個
Pivotグループ(Y=1に属する9個を一時的に格納する)→Pivotグループを90度回転

・回転終了
Pivotの中の9個の新しいワールド座標と回転を確定させ、元のシーン直下に戻す

位置と向きの2つを更新しないと、キューブに貼られているシールを回転させられない
initialPosで生まれつきのシールを保持

完成度を計算するロジック
ルービックキューブの外側には合計54枚のシール（色）がある
54個のうち、何枚が正しい方向を向いているか？

判定方法

1. あるピースの白いステッカーは、もともとは上方向(+Y)を向いていた
2. 回転させた結果、そのステッカーは今どちらを向いているか？
3. 今の向きを計算して、上方向(+Y)を向いているなら、カウント+1
4. 別を向いているならカウントされない

完成度 = (正解したステッカー数 / 54) \* 100(%)

30秒ごとに自動で回転するようにするには？

1. ランダムな手を選ぶ
2. 30秒のカウントダウンタイマー

サーバーも持たずにデータベースも使わずに、同じものが閲覧できるようにする
決定論的アルゴリズムを使う
アクセスされていない間も裏でサーバーが動いて回しているのではなく、
アクセスした瞬間の現在時刻から、過去にどんな回転が行われたかを全員同じように逆算して復元する

1. 基準となる開始日時
2. アクセスした時の現在時刻→経過時間を計算
3. 総回転数Nを算出 ?手目
4. シード付き擬似乱数で1手目~?手目の回転を瞬時に再現してキューブに反映

シード付き乱数(PRNG)とは？
通常のMath.random()はアクセスするたびにバラバラの値を出すが、シードを固定した乱数を使うと、何度計算しても全く同じ乱数列を生成できる

→リセットや手動操作を行うと、矛盾が発生してしまう

---

回転させたいとき、アニメーションをさせたいとき
一時的な回転台 Pivotグループを使う

1. 回転開始
   回す面にある9個のピースをシーンからPivotに一時的に載せ替える

2. アニメーション中
   Pivot自体を0度から90度までイージング(滑らかな加減速)をつけて回す
   →乗っている9個のピースも一緒に回る

3. 回転完了
   9個のピースの新しい座標と回転を確定し、Pivotからシーンに戻す
   Pivotの角度を0度にリセットする

必要
・現在アニメーション中の手というState
・キューブ描画コンポーネント(requestAnimationFrame)を使って、アニメーション実行


アニメーション崩れ
・パーツの欠落
group.children.forEachループの中でpivot.attach(child)を呼び、配列の要素を削除しながらループしていた
・ReactとThree.jsの不整合
命令的にattach/detachで親子関係を組み替えたため、React Three Fiberの仮想ツリーと実際の描画オブジェクトの対応が破綻した
・アニメーションのガタ付き
面全体が一枚の板として回らず、requestAnimationFrameがThree.jsの描画サイクルとずれていた

パーツをPivotに載せ替えて回すのをやめて、27個のパーツの位置は固定して、対象の9個の位置・角度だけを毎フレーム計算して直接動かすように変更

シーングラフの固定化 forwardRefを追加して、27個のメッシュの参照を保持
useFrameレンダーループの完全同期 生のrequestAnimationFrameでなく、Three.js R3F標準のuserFrameを採用
回転対象の層にいる9個のパーツにのみ、イージング関数を適用した回転クォータニオン？をリアルタイムに乗算
回転が100%に達した瞬間にapplyMoveToCubiesで座標を確定させる